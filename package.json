{
  "name": "careertalent-static",
  "version": "1.0.0",
  "private": true,
  "description": "TalentAI static site (HTML/CSS/JS) — no Node build step",
  "scripts": {
    "build": "echo Static site — no build required"
  }
}
